<!--
  Generated by the chapter-two design panel: four independent proposals, each
  attacked by a hostile reviewer that opened the actual source, then synthesised.
  Every code claim below was checked against the tree as it stood on 2026-07-26.
  Keep this file in step with what is actually built — see Docs/GAME_DESIGN.md
  for the story-level view.
-->

{
  "summary": "Design chapter two of the Korean horror prologue from four independent angles, adversarially verify each, then synthesise an implementation plan",
  "agentCount": 10,
  "logs": [
    "4/4 proposals survived critique"
  ],
  "result": {
    "plan": "# Chapter Two — 두 번째 아침\n## Implementation Plan (synthesised from all four proposals + reviews)\n\nAll paths are absolute under `C:/2026_Project/06_unreal`. Every code claim below was checked against the current source; line numbers cited are from the files as they exist today.\n\n---\n\n## 0. The one-sentence design\n\n> The morning restarts in-session at the villa's front door. The building has kept everything except its own geometry: 403 is no longer where it was, the corridor puts itself out behind you, the lift stops at a floor nobody pressed, the till has already rung up water you have not bought, and the chapter ends with you standing in a dark lobby listening to your own alarm four floors up.\n\nThe horror instrument set is deliberately narrow and all of it is buildable from `CreateBlock` + existing materials + existing audio factories. **One** new texture/material is introduced (`M_WetStep`). **Two** new C++ types are introduced. Everything else is an edit to a shipped file.\n\n---\n\n## 1. Decisions that override the proposals (with the code that forces them)\n\n| Proposal beat | Verdict | Evidence |\n|---|---|---|\n| 403 opens onto 404 itself (systems angle) | **Rejected** | `IGPrologueWorldScene.cpp:766` — 404's south wall spans X −220..98 and 404's own entrance opening is X 98..186 in the *same wall*. Two doors 2.9 m apart into one room. |\n| 403 diorama west of the stairwell (lore angle) | **Rejected** | The only wall segment there is `CreateBlock(FVector(-280,-225,120), FVector(120,20,240))` (`:1027`), and the floor in front of it is the stair flight — treads at X {−230,−252,−274,−296,−318} dropping 18 cm each (`:1116`) with a balustrade at Y=−243, X −334..−214 (`:1127`). A door there opens onto a staircase 45–63 cm below the landing. |\n| 403 at 4F-local X=−150 or −30, walk-in | **Rejected** | `DressUnitDoor` casing jambs sit at `DoorX ± 46` width 8 (`:1074-1075`) → clear opening exactly **84 cm**. `AIGPlayerCharacter` uses `InitCapsuleSize(42.0f, 96.0f)` (`IGPlayerCharacter.cpp:26`) → **84 cm** diameter. Zero clearance. |\n| **403 moves east of 404 (this plan)** | **Adopted** | X 220..690 / Y −215..+215 at 4F-local is genuinely empty: 404 ends at X=210 (`:763`), and the only thing in that band is the corridor's north-wall extension `CreateBlock(FVector(455,-225,120),(470,20,240))` (`:1028`). We split that block and author our own 102 cm casing. |\n| 2F landing built inside the lobby footprint | **Rejected** | The rider is teleported `-FloorDeltaZ` at the `ClosingUpper→Descending` transition (`IGElevator.cpp:449-458`), *before the first chime* — so any mid-ride door opening is the **lower** doors onto the lobby. Screening it fails: the common-entrance opening is X 600..686 in the south wall (`IGPrologueWorldScene.cpp:1215-1217`), and from an off-centre eye in the 150 cm cab a ray through the south gap edge crosses Y=−385 at X≈655, inside that opening. |\n| **2F stop = doors part 12 cm onto black, nothing, close (this plan)** | **Adopted** | Needs no landing, no third cab shell, no screen. We kill both lobby fluorescents first so the 12 cm slot is genuinely black. |\n| Lagging puddle reflection | **Cut** | No planar reflection / scene capture / render target anywhere in the module, and `AIGPlayerCharacter` has no body mesh, so a real reflection captures nothing to lag. The honest version is a puppet actor for one image on a leg we no longer walk. |\n| Ride home / \"404's door is open\" ending | **Cut** | `Docs/GAME_DESIGN.md:32` assigns that beat to **CH01**. And the teleport in `IGElevator.cpp:453` is one-directional; an ascending leg is a second teleport + new states, and `Docs/GAME_DESIGN.md` CH03 explicitly deletes the lift (`엘리베이터는 작동하지 않는다(정전). 계단으로만 이동`), so nothing downstream pays for it. **CH02 ends in the lobby.** |\n| CH02 autosaves | **Cut for this milestone** | `IGSaveSubsystem.cpp:10` — `AutosaveSlotCount = 2`, round-robin. `CreateSaveSnapshot` stores no player transform. `RequestLoad`/`ApplyLoadedProgress` have **zero callers** in the module and `Docs/IMPLEMENTATION_STATUS.md` lists the load coordinator as not done. Writing three CH02 checkpoints evicts both working CH01 slots with snapshots nobody can load. `Chapter.CH02` still gets declared so the tag exists when the coordinator lands. |\n| 여자 허밍 cue | **Cut** | `IGToneSequenceSoundWave.h` has exactly nine factories; `UIGAmbienceSoundWave` has three modes. No humming, no drip, no breathing. |\n\n### The one balance change that the whole chapter depends on\n\n`IGStressComponent.cpp:85` is `Stress += Darkness * DarknessRate * DeltaSeconds` with `DarknessRate = 0.055f` **per second**. Full darkness therefore reaches 0.5 in **9.1 seconds** and saturates at 1.0 in ~18 s. A chapter whose route is dark for minutes at a time pegs the meter and stops meaning anything.\n\n**Change `UIGStressComponent::DarknessRate` from `0.055f` to `0.012f`** (`IGStressComponent.h:80`). That gives 0.5 in ~42 s of full black and 1.0 in ~83 s — darkness becomes a floor-setter, not a driver, which is what makes the staircase shape achievable. Leave `RecoveryRate = 0.075f` alone: the asymmetry (slow rise, 13 s full drain) is exactly right for a route that alternates dark corridor / lit lift cab / lit store. CH01 is lit end-to-end and is unaffected.\n\nVerified recovery gate: `max(Darkness*0.5, ThreatPressure) < 0.05 && ScareCharge <= 0` → recovery needs `Darkness < 0.10`. Inside the lift cab, `CabLight` is 3400 cd at 430 cm radius (`IGElevator.cpp:143-144`); at ~100 cm the probe returns `3400*(1-100/430)²/1000 = 2.0`, clamped → **Darkness 0**. The cab is a genuine lull, for free.\n\n---\n\n## 2. New C++ types (exactly two)\n\n### `AIGSecondMorningDirector` — `Source/IndieGame/Sequence/IGSecondMorningDirector.{h,cpp}` (UTF-8 **with BOM**)\n\nMandatory. `AIGMorningRoutineDirector::RefreshPhase` early-returns on `NewPhase <= Phase` (`IGMorningRoutineDirector.cpp:151`), so a second loop cannot be new values on the same enum; its seven state tags are CH01 literals; and its `SetSceneReferences` carries only the store door, one alley lamp, the store lights and the jingle.\n\nOwns three things nothing else can:\n- Its own phase enum + `~Ascii` objective pair.\n- A **10 Hz timer** driving (a) the directional corridor kills, (b) `SetThreatPressure` re-assertion — `ThreatPressure` decays at 2.0/s (`IGStressComponent.cpp:100`) so every sustained hold must be re-asserted, (c) the ending sequencer. `AIGZoneTrigger` cannot do any of this: it cannot fire twice, cannot report exit, has no facing test and has no required-tag gate (`IGZoneTrigger.cpp` is 72 lines, all of it read).\n- Spawn-time gating of every CH02 zone. Because a tagless `AIGZoneTrigger` fires on any first entry in any chapter (see `FlickerZone`, `:2454-2457`), the only way to author \"CH02 only\" is to not spawn the zone until CH02 starts.\n\nPublic surface: `SetSceneReferences(...)` (extended), `GetPhase()`, `IsActive()`, `GetObjectiveText()`, `GetObjectiveTextAscii()`, `OnPhaseChanged`, `BeginSecondMorning()`.\n\n### `IIGObjectiveProvider` — `Source/IndieGame/Narrative/IGObjectiveProvider.{h,cpp}`\n\n`AIGHorrorHUD` holds `TWeakObjectPtr<AIGMorningRoutineDirector> MorningDirector` and both `GetObjectiveText`/`GetObjectiveTextAscii` are hard-typed to it (`IGHorrorHUD.h:93`, `.cpp:335-357`). Without this, CH02 draws **no** objective line — worse, `RefreshPhase` is monotone and `HandleStoryStateChanged` ignores removals, so CH01's `물을 샀다. …이제 집으로 돌아가자` would stay pinned on screen for the whole chapter.\n\nThree `BlueprintNativeEvent`-free virtuals: `GetObjectiveText() const`, `GetObjectiveTextAscii() const`, `IsObjectiveActive() const`. Implemented by both directors. CH03 gets it free.\n\n**No** new trigger class, note class, footprint class, silhouette class or sound-wave class.\n\n---\n\n## 3. Work order\n\nEach step compiles and leaves the game playable. Chapter one must still be completable after every step.\n\n---\n\n### Step 1 — Tags, balance, and two live bugs (no behaviour change to CH01)\n\n**`Config/DefaultGameplayTags.ini`** — append after line 53:\n```\n+GameplayTagList=(Tag=\"Chapter.CH02\",DevComment=\"Second morning chapter\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.HasWallet\",DevComment=\"Player took the wallet on the second morning\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.FridgeChecked\",DevComment=\"Player saw the empty fridge again\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.LeftHome\",DevComment=\"Player stepped into the dark corridor\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.CorridorDark\",DevComment=\"All four corridor fixtures are out\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.EnteredStore\",DevComment=\"Player entered the empty store\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.HasWater\",DevComment=\"Player is holding water on the second morning\")\n+GameplayTagList=(Tag=\"State.CH02.Loop.WaterPurchased\",DevComment=\"Player paid on the second morning\")\n```\n`Chapter.CH02` is required because `RequestGameplayTag(..., false)` returns an invalid tag under `ClearInvalidTags=True` and `RequestCheckpointAutosave` early-returns on invalid tags. `State.CH02.Loop.{Started, HasFlashlight, SawMirrorRoom, LiftStopped, ReadNotice, SawReceipt, Returned}` and `Checkpoint.CH02.*` already exist (lines 27–36) and are used as-is.\n\n**`Source/IndieGame/Player/IGStressComponent.h`** — line 80: `DarknessRate = 0.055f` → `0.012f`, with a comment explaining the arithmetic.\n\n**`Source/IndieGame/Core/IGPrologueWorldScene.cpp`** — two fixes:\n1. **Live bug**: `:2157` tests `if (HomeDoor)` but `HomeDoor` is not spawned until `:2176`, so the `M_DoorAd` sticker on the 404 door is never created. Move the `CreateDecoOnComponent` call to inside the `if (HomeDoor)` block at `:2180`.\n2. **Numbering**: `:1067` `NeighborPlates[] = {M_Plate401, M_Plate403}` against `NeighborDoorXs[] = {-30, -150}` puts 401 nearer 404 than 403, so the corridor currently reads west→east **403, 401, 404**. Swap to `{TEXT(\"M_Plate403\"), TEXT(\"M_Plate401\")}` so it reads **401 (X=−150), 403 (X=−30), 404 (X=143)**. This is load-bearing for step 6.\n\n**`Source/IndieGame/Player/IGFlashlightComponent.cpp`** — in `SampleFlicker`/`UpdateBattery`, the brown-out below 0.25 charge drops the beam to 12 % while `bOn` stays true, so `AIGPlayerCharacter::SampleAmbientDarkness` (`:127-130`) keeps returning 0.0 and the fear model reads \"fully lit\" during the darkest frames. Add a public `float GetEffectiveBeamFraction() const` (flicker × battery) and change the early-out in `SampleAmbientDarkness` to `if (Flashlight && Flashlight->IsOn() && Flashlight->GetEffectiveBeamFraction() > 0.35f) return 0.0f;`.\n\n---\n\n### Step 2 — Small public setters on shipped actors (additive, CH01 unaffected)\n\n| File | Add |\n|---|---|\n| `Interaction/IGElevator.h/.cpp` | `void ResetRide();` — clears `RideTimerHandle`+`ChimeTimerHandle`, `SetActorTickEnabled(false)`, `ApplyDoorOffsets(0,0)`, `State = IdleClosed`, `bRideComplete = false`, `ChimeFloor = 4`, `bIntermediateConsumed = false`. |\n| `Interaction/IGSwingDoor.h/.cpp` | `void SetOpenState(bool bNewOpen, bool bInstant);` — sets `bOpen`, then either `DoorPivot->SetRelativeRotation(FRotator(0, bOpen ? OpenYaw : 0, 0))` (instant) or `DoorAnimation.Begin(current, target, SwingDuration); SetActorTickEnabled(true)`. Needed for both the loop reset (re-close the front door) and the 403 beat. Note `SetOpenYaw` is already public and **is** the clamp — the leaf animates to `OpenYaw` and stops (`IGSwingDoor.cpp:298-303`); no chain-door subclass is required. |\n| `Interaction/IGFridge.h/.cpp` | `void SetInspectedStateTag(FGameplayTag);` and `void SetInspectionThought(const FText&);` and `void ResetInspection();` (clears `bInspectionDone`, closes the door). `bInspectionDone` is private with no clear and `BeginPlay` self-consumes on `HasState(InspectedStateTag)` (`IGFridge.cpp:355`). |\n| `Interaction/IGCheckoutCounter.h/.cpp` | `void SetStoryTags(FGameplayTag Required, FGameplayTag Purchased);` and `void SetPurchaseThought(const FText&);`. Both fields are `protected` with no setter and `CanInteract` returns false once `PurchasedStateTag` exists (`:92-96`). |\n| `Player/IGFlashlightComponent.h` | already has `SetAvailable`, `RefillBattery`, `AddImpulse` — no change beyond step 1. |\n\n---\n\n### Step 3 — The objective interface (CH01 keeps its objective line)\n\n**New** `Source/IndieGame/Narrative/IGObjectiveProvider.h/.cpp`.\n\n**`Sequence/IGMorningRoutineDirector.h`** — add `public IIGObjectiveProvider` and override the three methods (`GetObjectiveText`/`GetObjectiveTextAscii` already exist verbatim; `IsObjectiveActive()` returns `IsActive()`).\n\n**`Player/IGHorrorHUD.h/.cpp`**:\n- Replace `TWeakObjectPtr<AIGMorningRoutineDirector> MorningDirector` with `TWeakObjectPtr<AActor> ObjectiveDirector`.\n- `ResolveDirectors()` (`:298`) iterates `TActorIterator<AActor>` and takes the first that `Implements<UIGObjectiveProvider>()`. The existing 0.5 s `NextDirectorSearchTime` re-search (`:188-191`) already handles a destroyed-and-respawned director cleanly.\n- `GetObjectiveText()` / `GetObjectiveTextAscii()` call through the interface in the `FreeRoam` branch. **Keep the `Wake->GetWakeState()` switch exactly as it is** — CH02 respawns the wake director and replays the full flow, so `알람을 끄자` → `몸을 일으키자` → objective is correct for the second morning too.\n\nVerify at this point: CH01 still shows all six objective lines.\n\n---\n\n### Step 4 — Retained light arrays + the flicker gate (still no visible change)\n\n**`Source/IndieGame/Core/IGPrologueWorldScene.h`** — new private members:\n```cpp\nUPROPERTY(Transient) TArray<TObjectPtr<UPointLightComponent>>  CorridorLights;\nUPROPERTY(Transient) TArray<TObjectPtr<UStaticMeshComponent>>  CorridorLightDiscs;\nUPROPERTY(Transient) TArray<TObjectPtr<UPointLightComponent>>  LobbyLights;\nUPROPERTY(Transient) TArray<TObjectPtr<UStaticMeshComponent>>  LobbyLightDiscs;\nbool bCorridorFlickerSuspended = false;\n```\n\n**`IGPrologueWorldScene.cpp`**:\n- `BuildCorridor` fixture loop (`:1151-1168`): capture the `LightPanelMaterial` disc created at `:1156` (its component is currently **discarded**) into `CorridorLightDiscs`, and every light into `CorridorLights`. Killing a light without hiding its disc leaves a glowing ring on a black ceiling.\n- `BuildLobby` fixture loop (`:1254-1266`): same, into `LobbyLights` / `LobbyLightDiscs`.\n- `HandleCorridorFlicker` (`:1173`): first line becomes `if (bCorridorFlickerSuspended || !DegradedCorridorLight) return;`. Without this, `CorridorFlickerHandle` — a 0.09 s repeating timer started at `:717-722` for the **whole session** — overwrites any CH02 write to fixture 0 (which *is* `DegradedCorridorLight`, since the `FixtureX < 0` branch only matches X=−180) within 90 ms.\n- Add public methods on the scene, in a new `public:` section after `BeginPlay()`:\n```cpp\npublic:\n    void SuspendCorridorFlicker(bool bSuspend);\n    void SetFixtureLive(int32 Index, bool bLive, bool bCorridor); // intensity + disc material\n    const TArray<TObjectPtr<UPointLightComponent>>& GetCorridorLights() const;\n    // ...and the CH02 entry points added in steps 5-9.\n```\n`AIGPrologueWorldScene` is `UCLASS(NotBlueprintable, Transient)` with everything after `BeginPlay()` private, and subclassing buys nothing — so **all CH02 geometry is authored inside this class** and the director reaches it through a small, explicit public surface. Do not promote the geometry helpers.\n\n---\n\n### Step 5 — The CH02 overlay container (built hidden; still no visible change)\n\n**`IGPrologueWorldScene.h`** — add:\n```cpp\nUPROPERTY(Transient) TArray<TObjectPtr<UStaticMeshComponent>> Ch02Components;   // hidden in CH01\nUPROPERTY(Transient) TArray<TObjectPtr<UStaticMeshComponent>> Ch01OnlyComponents; // hidden in CH02\nUPROPERTY(Transient) TArray<TObjectPtr<UPointLightComponent>> Ch02Lights;\nbool bChapterTwoActive = false;\nvoid SetChapterTwoOverlayVisible(bool bVisible); // public\n```\n`SetChapterTwoOverlayVisible` must flip **both** visibility *and* collision — `CreateBlock` sets `BlockAll` and `SetVisibility(false)` leaves the collision behind, which would put an invisible wall in the 403 doorway. Use `SetVisibility()` + `SetCollisionEnabled(QueryAndPhysics | NoCollision)`.\n\nAdd a helper `UStaticMeshComponent* CreateOverlayBlock(...)` that forwards to `CreateBlock`, pushes into `Ch02Components`, and starts hidden + non-colliding.\n\nNote the mobility constraint: `CreateBlock` sets `EComponentMobility::Static` (`:286`). Static components can be hidden/shown, so this is fine; do **not** try to move them.\n\n---\n\n### Step 6 — The mirror unit (403) geometry\n\n**`IGPrologueWorldScene.cpp`, `BuildCorridor`** — replace `:1028`:\n```cpp\nCreateBlock(FVector(455, -225, 120), FVector(470, 20, 240), CorridorWallX);\n```\nwith four blocks leaving a **102 cm × 194 cm** opening centred on X=453:\n```cpp\nCreateBlock(FVector(311, -225, 120), FVector(182, 20, 240), CorridorWallX);   // X 220..402\nCreateBlock(FVector(597, -225, 120), FVector(186, 20, 240), CorridorWallX);   // X 504..690\nCreateBlock(FVector(453, -225, 217), FVector(102, 20,  46), CorridorWallX);   // head, Z 194..240\nCh01OnlyComponents.Add(\n    CreateBlock(FVector(453, -225,  97), FVector(102, 20, 194), CorridorWallX)); // the plug\n```\nThe plug is the same material in the same plane — in CH01 the wall is visually and physically unchanged.\n\n**New private stage `BuildMirrorUnit()`**, called from `InitializePrologue` between `BuildCorridor()` and `BuildLobby()`, with `ActiveParent = UpperFloorRoot`. Everything it creates goes into `Ch02Components` / `Ch02Lights`.\n\nVolume (4F-local): interior **X 220..600, Y −215..+215, Z 0..230** — the same 380 × 430 × 230 as 404.\n```\nfloor        (410,   0, -10)  (400, 490,  20)  M_Jangpan\nceiling      (410,   0, 240)  (400, 490,  20)  M_WallpaperCeil\nwest wall    (215,   0, 115)  ( 10, 490, 230)  M_Wallpaper_Y   // fills the X 210..220 gap\neast wall    (605,   0, 115)  ( 10, 490, 230)  M_Wallpaper_Y\nnorth wall   (410, 225, 115)  (400,  20, 230)  M_Wallpaper_X\n```\n(The void at X 610..690 north of the corridor wall is unreachable and unlit; leave it.)\n\n**Contents rule.** Mirror-unit X = 404-local X **+ 410**, except the entrance cluster, which is placed relative to the mirror door at X=453 using its 404 offsets from X=143. Y and Z are unchanged. Reduced copy — ~35 blocks, not a `BuildApartment` refactor. In a room lit only by a torch the silhouette is what reads, and a hand-authored copy is what lets it be *subtly* wrong.\n\n| 404 | Mirror unit | Notes |\n|---|---|---|\n| bed `(-140,110)` | `(270,110)` | `PlacePhotoProp(\"old_bed_frame\", (270,110,0), (108,208,100), 90)` + greybox fallback |\n| duvet `(-140,110,47)/(‑140,60,60)/(‑140,118,63)` | see below | **mound variant** |\n| desk `(-140,-185)` | `(270,-185)` | `metal_office_desk`, target `(104,58,78)` |\n| chair `(-140,-138)` | `(270,-138)` | `painted_wooden_chair_01` |\n| wardrobe `(-170,-105)` | `(240,-105)` | `modern_wooden_cabinet`, yaw 90 |\n| bedside table `(-160,-35)` | `(250,-35)` | `side_table_01` |\n| kitchen run X 133..190 | X 543..600 | three blocks only: carcass `(572,128,46)(56,176,72)` Gloss, worktop `(571,128,84.5)(60,176,5)` CounterStone, wall units `(584,128,178)(32,176,68)` Gloss |\n| window X −163..−37 @ Y 212 | X 247..373 @ Y 212 | same 6 frame blocks, but the glow plane uses **`WindowDarkMaterial`**, not `WindowGlowMaterial` |\n| shoe cabinet `(48,-198)` | `(358,-198)` | 143 → 453 offset: `48−143 = −95` → `453−95 = 358`. Keeps it clear of the doorway. |\n| entrance step `(143,-195,4)` | `(453,-195,4)` | the 현관 step, correctly inside the door |\n| baseboard `(-46,-213,5)(286,4,10)` | two pieces X 222..400 and X 506..598 | avoids the doorway |\n\n**The sleeper** (no skeletal mesh, no humanoid asset, no animation exists anywhere in the module — do not design past this):\n```\n(270,110, 47) (94,194,18)  BeddingUV      // mattress, as 404\n(270, 88, 66) (86,116,26)  BeddingUV      // torso ridge\n(270,140, 70) (82, 42,30)  BeddingUV      // shoulder\n(268,168, 64) (22, 24,20)  PlasticDarkMaterial, SphereMesh   // hair, on the pillow, facing +Y\n```\nIt never moves, is never directly lit, and is never approached closer than the foot of the bed. The head end is +Y (matching `old_bed_frame` at yaw 90 with the headboard at Y≈211), so it faces away from the door.\n\n**The lamp**: `Ch02Lights.Add(CreateLight(FVector(250,-35,110), 300.0f, 340.0f, FLinearColor(1.0f,0.45f,0.18f), true, 4.0f))`. This is the warm bar under the open door — the only warm thing in a dead corridor. Do **not** claim it relieves darkness: at the corridor door plane the probe reads `300 × (1−310/340)² / 1000 ≈ 0.002`, i.e. nothing. It is art direction, not a mechanic.\n\n**Casing + plate** (CH02 only, `Ch02Components`):\n```\njamb  (400, -233, 100) (  8, 7, 200)  M_Stucco_X\njamb  (506, -233, 100) (  8, 7, 200)  M_Stucco_X\nhead  (453, -233, 202) (114, 7,   8)  M_Stucco_X\nplate (453,-231.5,212) ( 16, 2,   8)  M_Plate403\n```\n\n**Hide the old 403.** Push the eleven dressing components emitted by `DressUnitDoor(-30, -234.5)` plus that iteration's four casing/plate blocks into `Ch01OnlyComponents`. In CH02, X=−30 is blank stucco. Refactor `DressUnitDoor` to return the components it made, or simply record `GeometryComponents.Num()` before and after each `NeighborIndex` iteration and slice.\n\n> **This is the chapter's centrepiece and it costs one `SetVisibility` loop plus 40 blocks.** In CH01 the corridor reads 401 · 403 · 404 · blank wall. In CH02 it reads 401 · blank wall · 404 · **403**. The unit moved. The player walked past that wall four minutes ago.\n\n**The 403 door actor** is spawned in step 8 (it needs `SpawnInteractables` context).\n\n---\n\n### Step 7 — Wet-step material and the footprint quads\n\n**`Scripts/generate_surface_textures.py`** — add a `M_WetStep` source texture: two soft dark ellipses (ball + heel) on transparent-black, ~256×128.\n**`Scripts/create_textured_materials.py`** — add the matching material.\n**`IGPrologueWorldScene.cpp:404-437`** — append `TEXT(\"M_WetStep\")` to `MaterialNames[]`. `TexMat` silently returns the fallback for any name not in that array, so the failure mode is grey rectangles with no warning. Call site: `TexMat(TEXT(\"M_WetStep\"), PlasticDarkMaterial)` — a dark quad still reads as a wet print if the scripts have not been re-run.\n\n**New private stage `BuildChapterTwoFootprints()`** (called from `InitializePrologue`, all `Ch02Components`). `CreateBlock(..., FVector(26,11,1.2), WetStep, /*bEnableCollision*/false, nullptr, FRotator(0, Yaw, 0))`. `CreateBlock` auto-disables shadow casting below 3 cm (`:296`) — that is the intended decal path; **no** `UDecalComponent` is used anywhere in the module and adding one is a new rendering path for no gain.\n\nThree runs, tracked in three separate arrays so the director can reveal them independently:\n- **`CabPrints`** — lower cab floor, world Z 1.2 (lower cab base is Z 0). Six prints walking in from the door plane and stopping short of centre: `(700,-318)`, `(716,-292)`, `(732,-318)`, `(748,-292)`, `(764,-318)`, `(778,-300)`, all yaw 0 (toes toward +X, i.e. facing the rear of the car — whoever it was came in and turned around). Cab interior is X 715..865 / Y −380..−230; clamp to X ≥ 716.\n- **`LobbyPrints`** — lobby granite, world Z 0.6, leading from the lift doors out to the common entrance: `(690,-305)`, `(672,-292)`, `(654,-318)`, `(636,-292)`, `(618,-318)`, `(610,-352)`, `(606,-376)`.\n- **`AlleyPrints`** — asphalt, world Z 0.9, six prints heading east from the porch and stopping in open dry ground: `(650,-430)` … `(760,-448)`, scaled down 26→21 cm across the run so they dry out.\n\n---\n\n### Step 8 — The loop reset\n\n**Extract, don't duplicate.** In `IGPrologueWorldScene.cpp`, pull four blocks into new private methods so the reset runs the same code path as the first morning:\n- `SpawnMorningActors()` ← lines 2485–2534 (`AlarmSound`, `AlarmClock`, `WakeDirector`, `GetUpTarget`)\n- `SpawnWallet()` ← lines 2319–2333\n- `SpawnWaterBottles()` ← lines 2370–2409\n- `SpawnStoreZones()` ← the `StoreEntryZone` spawn\n\n**New public method `void AIGPrologueWorldScene::BeginSecondMorning()`**, in this exact order:\n\n```\n 1. PlayerCameraManager->StartCameraFade(0,1, 1.2f, Black, false, /*bHoldWhenFinished*/true)\n 2. timer 1.30 s -> ApplySecondMorningSwap()\n```\n`ApplySecondMorningSwap()`:\n```\n 1. MorningDirector->Destroy();          // EndPlay unbinds OnStoryStateTagChanged and\n                                          // ClearAllTimersForObject(this) — must be FIRST\n 2. StoryState->ClearStates(/*bBroadcastChanges*/false);   // silent: no dispatch, no re-entrancy\n 3. AlarmClock->Destroy(); WakeDirector->Destroy(); GetUpTarget->Destroy();\n 4. Wallet->Destroy();  for (b : WaterBottles) b->Destroy();  WaterBottles.Reset();\n       // AIGPickupItem::FinishPickup leaves bPickedUp/hidden/disabled with no reset path\n       // (IGPickupItem.cpp:131-178). Destroying the carried bottle is safe:\n       // AIGPlayerCharacter::UpdateCarriedItem null-checks CarriedActor (IGPlayerCharacter.cpp:186).\n 5. SpawnWallet();  Wallet->StateTagOnPickup = State.CH02.Loop.HasWallet;\n       Wallet->ThoughtOnPickup = <CH02 line>\n    SpawnWaterBottles(); for each: StateTagOnPickup = State.CH02.Loop.HasWater\n 6. Fridge->ResetInspection();\n    Fridge->SetInspectedStateTag(State.CH02.Loop.FridgeChecked);\n    Fridge->SetInspectionThought(<CH02 line>);\n 7. HomeDoor->SetOpenState(false, /*bInstant*/true);\n    HomeDoor->SetRequirements({ CH02.FridgeChecked, CH02.HasWallet });   // MANDATORY:\n       // without it the cleared CH01 tags leave the two shipped requirements\n       // (:2194-2214) permanently unmet and the player is sealed inside 404.\n    BuildingDoor->SetOpenState(false, true);\n 8. Checkout->SetStoryTags(CH02.HasWater, CH02.WaterPurchased);\n    Checkout->SetPurchaseThought(<CH02 line>);\n 9. Elevator->ResetRide();\n    Elevator->SetIntermediateStop(2, /*gap cm*/12.0f, /*dwell s*/5.0f);\n10. Torch: Player->GetFlashlight()->SetAvailable(true); RefillBattery(1.0f);\n    Flashlight pickup actor -> SetActorHiddenInGame(true), SetInteractionEnabled(false)\n       // CH02 is dark in both directions; the torch must never be missable.\n       // BatterySeconds is 420 s of continuous use, comfortably over the route.\n11. SetChapterTwoOverlayVisible(true);\n    SuspendCorridorFlicker(true);\n    SetFixtureLive(0, false, /*bCorridor*/true);   // the west end is already dead\n12. PositionPlayer();                              // reuses the existing private method\n13. SpawnMorningActors();                          // fresh alarm (bHasCompleted=false)\n                                                   // fresh wake director (WakeState=NotStarted)\n14. StoryState->AddState(State.CH02.Loop.Started);\n15. spawn AIGSecondMorningDirector, SetSceneReferences(...), it spawns all CH02 zones\n16. WakeDirector->StartWakeUp();                   // owns the fade-in from black (2.2 s)\n```\n\nSteps 3/13 are the point: `AIGAlarmClock::StartAlarm` early-returns on `bHasCompleted` (`IGAlarmClock.cpp:47`) and `AIGWakeUpDirector::StartWakeUp` early-returns unless `NotStarted` (`IGWakeUpDirector.cpp:163`); both flags are private with no reset. A **fresh actor** has neither. `AIGPrologueWakeDirector::ConfigurePrototype` sets `bAutoStart = false`, and step 2 already cleared `State.CH01.Wake.Standing`, so `BeginPlay`'s `RestoreStandingCheckpoint()` branch (`IGWakeUpDirector.cpp:~265`) does not fire and our explicit `StartWakeUp()` runs the flow from the top.\n\n**The boundary zone.** Spawned by a tiny handler on the scene bound to `UIGStoryStateSubsystem::OnStoryStateTagChanged`: when `State.CH01.Morning.WaterPurchased` is **added**, spawn `LoopBoundaryZone` at world `(643, -420, 110)` half-extent `(70, 40, 110)`, no state tag, no thought, delegate → `BeginSecondMorning`. Spawn-time gating is the only way to make a zone CH02-specific.\n\nPlacing it at the **villa common entrance** rather than at the store door:\n- lets CH01's ghost-chime beat run in full (a 2.3 s timer plus a thought at +1.4 s, `IGMorningRoutineDirector.cpp:257-263`);\n- honours CH01's own `Complete` objective, `물을 샀다. …이제 집으로 돌아가자`;\n- does not consume `Docs/GAME_DESIGN.md:32`'s CH01 ending (the player never reaches 404).\n\n**`Sequence/IGDemoDirector.cpp` / `.h`** — `ConfigureDemo` holds raw pointers to `WakeDirector`, `AlarmClock`, `Fridge`, `Wallet`, `HomeDoor`, `Elevator`, `BuildingDoor`, water bottle and `Checkout` (`IGPrologueWorldScene.cpp:2634-2646`), three of which the reset destroys. `-IGCapture` is the project's only verification harness (see the build-pipeline memory). Guard it: `SpawnDemoDirectorIfRequested()` sets a flag on the scene, and `BeginSecondMorning()` early-returns when the demo director is alive. The capture tour stays a CH01 tour.\n\n---\n\n### Step 9 — `AIGSecondMorningDirector`\n\nPhase enum, pure function of tags, monotone like CH01's:\n```\nInactive → CheckFridge → TakeWallet → ToLift → ToStore → FindWater → PayAtCounter → GoHome → Complete\n```\nderived from `State.CH02.Loop.{FridgeChecked, HasWallet, LeftHome, EnteredStore, HasWater, WaterPurchased, Returned}`.\n\n`SetSceneReferences(AIGPrologueWorldScene* Scene, AIGElevator*, AIGSwingDoor* MirrorDoor, AIGSlidingDoor* StoreDoor, TArray<UPointLightComponent*>&& StoreLights, UAudioComponent* Jingle)`. **Snapshot base intensities exactly once**, at the first call — `AIGMorningRoutineDirector::SetSceneReferences` re-snapshots `Light->Intensity` on every call and is invoked twice (`:2550`, `:2601`); do not repeat that.\n\n`bAutosaveAtCheckpoints = false` with a `// TODO(load-coordinator)` comment citing `IGSaveSubsystem.cpp:10`.\n\n**10 Hz timer** (`SetTimer(..., 0.1f, true)`) does four things:\n\n**(a) Corridor kills.** Fixtures at 4F-local X {−180, 60, 300, 540}; fixture 0 is dark from the reset. Rule for i ∈ {1,2,3}: kill when `PlayerZ > 1000 && PlayerX > FixtureX + 140` **and** at least **3.0 s** since the previous kill. Thresholds land at X≈200 / 440 / 680; at `MaxWalkSpeed = 300` cm/s (`IGPlayerCharacter.cpp:35`) that is 1.6 s of walking, so the dwell gate is what stops it reading as a strobe — kills 2 and 3 land while the player is stopped at the 403 door or waiting for the lift, behind them, down a corridor they have finished walking.\n\nKill = `Light->SetIntensity(0)` + `Disc->SetMaterial(0, PlasticDarkMaterial)` + one-shot at the fixture location. **Reuse `UIGToneSequenceSoundWave::CreateFootstep(this, 2.6f, 0.55f)`** as the ballast snap — a short high noise burst; no new factory. Then `Stress->ApplyScare(0.10 / 0.14 / 0.18)` (these are the first callers of `ApplyScare` anywhere in the module) and `Flashlight->AddImpulse(FRotator(-1.8f, 2.4f, 0))` so the beam jumps.\n\nOn the third kill: `State.CH02.Loop.CorridorDark`.\n\n**(b) `SetThreatPressure` holds** — re-asserted every tick, never a one-shot: 0.30 for 18 s after the 403 door closes, 0.45 across the 5 s lift hold, 0.25 while the receipt panel is open, 0.40 through the ending.\n\n**(c) The 403 sightline.** Once, when `dist(player, mirrorDoor) < 260` and `Dot(CameraForward, ToDoor) > 0.86` and the torch is on: push the corridor thought. (A zone fires on box entry regardless of facing and cannot re-arm.)\n\n**(d) The ending sequencer** (step 12).\n\n**Zones the director spawns at `BeginPlay`** (all `AIGZoneTrigger`, all therefore inert during CH01 by construction):\n\n| Name | World location | Half-extent | Tag | Delegate |\n|---|---|---|---|---|\n| `Ch02LeftHomeZone` | `(150,-300,1010)` | `(60,50,110)` | `State.CH02.Loop.LeftHome` | arms the corridor kills |\n| `Ch02MirrorRoomZone` | `(453,-160,1010)` | `(60,45,110)` | `State.CH02.Loop.SawMirrorRoom` | closes the 403 door |\n| `Ch02EntranceZone` | `(620,-400,10)` | `(70,45,110)` | — | salt thought |\n| `Ch02StoreEntryZone` | `(2450,-457,116)` | `(35,95,110)` | `State.CH02.Loop.EnteredStore` | double chime |\n| `Ch02ReturnZone` | `(600,-300,110)` | `(90,70,110)` | `State.CH02.Loop.Returned` | ending |\n\n---\n\n### Step 10 — The 403 door beat\n\n**`SpawnInteractables`** — spawn `MirrorDoor` (an ordinary `AIGSwingDoor`) at world `(402, -225, 900)`, rotation `FRotator(0,-90,0)` (same recipe as `HomeDoor` at `:2176-2179`, whose hinge is on the west jamb with the leaf extending east). `ConfigurePrototypeVisuals(CubeMesh, M_SteelDoorUV, M_StainlessUV, FVector(7, 100, 192))`, `SetLeverMesh(SM_LeverHandle, ...)`, `SetOpenYaw(-75.0f)`, **no requirements**, `SetActorHiddenInGame(true)` + `SetActorEnableCollision(false)` until the overlay reveals it.\n\nAt the reset: `MirrorDoor->SetOpenState(true, /*bInstant*/true)` — it stands 75° open, throwing the warm lamp bar across the dark corridor tile. It is the only light left on the floor and the player walks toward it.\n\n`Ch02MirrorRoomZone` fires when they step inside:\n1. `MirrorDoor->SetOpenState(false, /*bInstant*/false)` — 1.0 s swing with `CreateDoorCreak`. **No `CreateDoorThud`.** `AIGSwingDoor::CompleteInteraction` already plays the creak; call the audio explicitly from the director so the close is not silent.\n2. Kill `Ch02Lights[0]` (the mirror bedside lamp).\n3. `ApplyScare(0.30)`, then `SetThreatPressure(0.30)` held 18 s.\n4. Two thoughts (below).\n\nThe door is **not locked and has no requirements**: the player opens it and walks out. Nothing chases them. That refusal is the beat.\n\n---\n\n### Step 11 — The lift stops at 2\n\n**`Interaction/IGElevator.h`** — add:\n```cpp\nDECLARE_DYNAMIC_MULTICAST_DELEGATE_TwoParams(FIGElevatorFloorSignature, AIGElevator*, Elevator, int32, Floor);\n\nvoid ResetRide();                                                   // step 2\nvoid SetIntermediateStop(int32 Floor, float GapCentimeters, float DwellSeconds);\nUPROPERTY(BlueprintAssignable, Category=\"Elevator|Events\") FIGElevatorFloorSignature OnFloorReached;\n```\nprivate: `int32 IntermediateStopFloor = INDEX_NONE; float IntermediateGap = 0.f; float IntermediateDwell = 0.f; bool bIntermediateConsumed = false; FTimerHandle DwellTimerHandle;` and three enum values `PartingMid, HoldingMid, ClosingMid`.\n\n**`IGElevator.cpp`**:\n- `HandleFloorChime` (`:494`): after `--ChimeFloor` and before the `< 1` branch, insert\n```cpp\nOnFloorReached.Broadcast(this, ChimeFloor);\nif (ChimeFloor == IntermediateStopFloor && !bIntermediateConsumed)\n{\n    bIntermediateConsumed = true;\n    GetWorldTimerManager().ClearTimer(ChimeTimerHandle);\n    SetState(EIGElevatorState::PartingMid);\n    DoorAnimation.Begin(0.0f, IntermediateGap * 0.5f, 0.5f);\n    SetActorTickEnabled(true);\n    return;\n}\n```\n(Both leaves move — `ApplyDoorOffsets` places each panel at `Side * (DoorPanelWidth*0.5 + Offset)`, `:534-541` — so the visible gap is `2 × Offset`. Passing 12 cm yields a 12 cm gap.)\n- `Tick` (`:419-430`): add `case PartingMid: case ClosingMid: ApplyDoorOffsets(0.0f, DoorAnimation.CurrentValue); break;`\n- `Tick` finish switch (`:438`): `PartingMid → SetState(HoldingMid)` + `SetTimer(DwellTimerHandle, ..., IntermediateDwell, false)` → `SetState(ClosingMid); DoorAnimation.Begin(IntermediateGap*0.5f, 0, 0.5f); SetActorTickEnabled(true)`. `ClosingMid → SetState(Descending)` and re-arm `ChimeTimerHandle` at `SecondsPerFloor`, repeating.\n\n**Director, on `OnFloorReached(2)`:**\n1. Kill **both** lobby fluorescents *and* their emissive discs (`LobbyLights` / `LobbyLightDiscs`) — the 12 cm slot must show black, and the discs would otherwise hang lit on a dark ceiling.\n2. Hold `SetThreatPressure(0.45)` across the whole 5 s, at 10 Hz.\n3. At t+4.4 s (just before the doors close): reveal `CabPrints`, `ApplyScare(0.22)`, push the print thought, set `State.CH02.Loop.LiftStopped`.\n4. On `OnArrivedAtLobby`: reveal `LobbyPrints` and `AlleyPrints`; bring the X=510 fluorescent (mailboxes) back at **0.25×** and leave X=660 dead.\n\nNote the alley streetlight at X=1000 is already permanently dead from CH01 — `HandleFlickerStep` ends with `FlickerLight->SetIntensity(0.0f)` (`IGMorningRoutineDirector.cpp:358`) and `bFlickerConsumed` is never cleared. CH02's alley is darker than CH01's for free. Do **not** try to re-trigger `FlickerZone`: it carries no state tag, so `bTriggered` latched for the session and it has no re-arm.\n\n---\n\n### Step 12 — Store, entrance tableau, and the ending\n\n**Store** (director-side, no geometry):\n- `Ch02StoreEntryZone` → `StoreDoor->PlayChime(0.85f)` twice, 0.8 s apart.\n- Kill the two north-row `StoreLights` (X {2540, 2820} at Y=−300).\n- **Receipt**: a second `AIGReadableNote` spawned by the director at world `(2596, -268, 100.2)`, `ConfigurePrototypeVisuals(CubeMesh, TexMat(\"M_PaperClean\", SignWhiteMaterial), FVector(8.0f, 16.0f, 0.6f))` — thin axis is Z so it lies flat on the counter top (Z 99). The management notice is 20 m away on 4F, so the single-open-note static (`IGReadableNote.cpp:7`) can never contend.\n  - `OnReadStateChanged(bOpened=true)` → `State.CH02.Loop.SawReceipt`, `SetThreatPressure(0.25)` held while open.\n  - `OnReadStateChanged(bOpened=false)` → `ApplyScare(0.20)`, all store lights permanently to 0.30×, `JingleComponent->SetPaused(true)` permanently.\n- The till **works** in CH02 (retagged in step 8). The receipt already lists the purchase; paying prints the fourth line that is already there.\n\n**Common entrance tableau** (geometry, `Ch02Components`, `BuildChapterTwoOverlay()`): a line of ~40 2 cm `SignWhiteMaterial` cubes scattered across world `(604, -390, 1)` (grains read better than a stripe), plus a low offering just outside at `(632, -418, 0)`: bowl = `CylinderMesh` `(632,-418,4)(18,18,8)` Metal, rice = `(632,-418,8.5)(15,15,3)` SignWhite, **spoon standing upright** = `(632,-418,17)(1.6,1.6,18)` Metal, cup `(650,-418,4)(7,7,8)` SignWhite, three burnt incense sticks `(618,-418,6)(0.8,0.8,12)` PlasticDark. One `AIGInspectable` on the offering.\n\n**Management notice**: at the reset, `ManagementNotice->SetNoteText(...)` with the CH02 body (below). No new actor, no new material.\n\n**Mailbox**: one `AIGInspectable` at world `(500, -224, 131)` on the existing 403 box block (`:1228-1237`).\n\n**Ending** (`Ch02ReturnZone` in the lobby → director sequencer):\n```\nt+0.0  State.CH02.Loop.Returned; ThreatPressure 0.40 held\nt+1.6  kill both lobby fluorescents + discs; StopDrone()\nt+3.4  water drop: a (2,2,6) M_WetStep quad animated from world (600,-300,238)\n       down to Z 2 over 0.42 s, plus a (14,14,0.6) splash quad revealed on impact,\n       plus a damp patch (34,34,0.6) M_PaperWet on the ceiling above it.\n       CreateFootstep(this, 3.4f, 0.30f) as the tick of the impact.\nt+4.0  thought\nt+6.2  distant alarm — see below\nt+8.4  thought\nt+11.0 StartCameraFade(0,1, 2.6f, Black, false, /*bHold*/true); DistantAlarm->Stop()\n```\nThe lift is honestly dead: `ResetRide()` is **not** called a second time, so `State == Done` and `CanInteract` fails on `State != IdleClosed` (`IGElevator.cpp:353-357`). The call button does nothing because the lift does nothing.\n\n**The distant alarm must not leak.** `UIGAlarmSoundWave` is constructed with `bLooping = true` and `INDEFINITELY_LOOPING_DURATION`, and `IGAudio::SpawnOneShotAt` is fire-and-forget. Hold the handle:\n```cpp\nDistantAlarm = UGameplayStatics::SpawnSoundAtLocation(\n    this, AlarmSound, FVector(790, -305, 900), FRotator::ZeroRotator,\n    0.30f, 1.0f, 0.0f, /*AttenuationSettings*/nullptr,\n    /*ConcurrencySettings*/nullptr, /*bAutoDestroy*/false);\n```\nand `Stop()` it on the fade and in `EndPlay`.\n\n---\n\n## 4. Final Korean copy\n\nRegister: 30-something man alone at 4:44, tired, under-reacting, talking to himself. Flat statements ending in `-네 / -는데 / -지`. No exclamation marks, no narration of what the player can already see, no invented proverbs.\n\n### Objectives — `AIGSecondMorningDirector::GetObjectiveText()`\n\n| Phase | Korean | ASCII fallback |\n|---|---|---|\n| CheckFridge | `또 목이 마르다 — 냉장고를 확인하자` | `Thirsty again - check the fridge` |\n| TakeWallet | `지갑을 챙기자` | `Grab the wallet` |\n| ToLift | `엘리베이터까지 가자` | `Get to the elevator` |\n| ToStore | `골목 끝 편의점에서 물을 사 오자` | `Buy water at the corner store` |\n| FindWater | `음료 냉장고에서 생수를 찾자` | `Find bottled water in the cooler` |\n| PayAtCounter | `카운터에서 계산하자` | `Pay at the counter` |\n| GoHome | `집으로 돌아가자` | `Head back home` |\n| Complete | *(empty)* | *(empty)* |\n\n> `골목 끝 편의점에서 물을 사 오자`, `음료 냉장고에서 생수를 찾자` and `카운터에서 계산하자` are **byte-identical to chapter one on purpose** — the repetition is the loop made textual. Comment this at the definition site or it will be flagged as a copy-paste error in review.\n\n### Thought lines\n\n| When | Korean | Duration |\n|---|---|---|\n| 1.8 s after the alarm is stopped | `…또 4시 44분이네.` | 3.4 |\n| +1.6 s | `어제도 이 시간이었는데.` | 3.6 |\n| Torch found already in pocket (at wake) | `…손전등이 왜 주머니에 있지.` | 3.6 |\n| Fridge opened (`SetInspectionThought`) | `또 없네. 어제 사 왔는데.` | 4.2 |\n| Wallet taken (`ThoughtOnPickup`) | `지갑. 어제 쓴 게 그대로 있네.` | 3.6 |\n| `Ch02LeftHomeZone` | `복도 끝이… 아예 깜깜하네.` | 3.6 |\n| 2nd corridor kill | `뒤에서부터 꺼지네.` | 3.2 |\n| 3rd corridor kill | *(silence — deliberate)* | — |\n| 403 sightline test passes | `403호… 저쪽 아니었나.` | 4.0 |\n| `Ch02MirrorRoomZone` | `…여긴 우리 집인데.` | 3.8 |\n| +3.0 s, after the door closes | `…깨우면 안 될 것 같다.` | 4.0 |\n| 1.2 s after the lift doors part at 2 | `2층은 누른 적도 없는데.` | 3.4 |\n| Cab prints revealed | `…이거, 언제 생겼지.` | 3.6 |\n| Mailbox inspectable | `403호 우편함인데, 고지서가 전부 404호 앞으로 와 있네.` | 4.4 |\n| `Ch02EntranceZone` | `현관에 소금을 왜 뿌려 놨지.` | 3.6 |\n| Offering inspectable | `밥에 숟가락은 저렇게 꽂는 게 아닌데.` | 4.2 |\n| `Ch02StoreEntryZone` | `…'어서 오세요'가 두 번 났는데.` | 3.8 |\n| Water taken (`ThoughtOnPickup`) | `차갑다. 이거면 됐어.` *(CH01 line, reused on purpose)* | 3.4 |\n| Receipt panel closed | `오늘 건 아직 안 샀는데.` | 3.8 |\n| Checkout (`SetPurchaseThought`) | `찍고, 돈 놓고. …영수증은 안 나왔다.` | 4.6 |\n| Ending, water drop | `…위에서 물이 떨어지는데.` | 3.8 |\n| Ending, distant alarm | `…우리 집 알람인데.` | 4.4 |\n\n### CH02 management notice — `ManagementNotice->SetNoteText(...)`\n\nTitle: `[관리사무소] 단수 안내`\n\n```\n입주민 여러분께 알려 드립니다.\n\n옥상 물탱크 청소 및 수질 점검 관계로\n아래와 같이 단수를 실시합니다.\n\n   일시 :  7월 26일 (금)  04:00 ~ 06:00\n   대상 :  404호\n\n점검 중에는 옥상 출입을 삼가 주시기\n바랍니다. 불편을 드려 죄송합니다.\n\n※ 현관에 소금 뿌리신 분, 치워 주세요.\n\n            달빛빌라 관리사무소\n```\nThe only changed line is `대상`, from `전 세대 (401호 ~ 404호)` to `404호` — the notice the player already read once, now addressed to one unit. Plus the weary handwritten postscript that collides superstition with property management. Body lines go in as separate `FText` entries; blank entries are paragraph breaks. Thirteen lines is the working budget for `DrawNotePanel`.\n\n### CH02 receipt — the second `AIGReadableNote`\n\nTitle: `판매 내역 · 새벽편의점`\n\n```\n07-26 (금)  04:51\n-----------------------\n  생수 500mL     1     950\n-----------------------\n  합  계               950\n  현  금               950\n  거스름돈               0\n-----------------------\n  07-25 (목) 04:51   생수 500mL\n  07-24 (수) 04:51   생수 500mL\n  07-23 (화) 04:51   생수 500mL\n-----------------------\n  담당 :  -\n```\n\nThree constraints baked into this text, all of them fixes for problems reviewers found:\n- **950원**, matching the shipped prompt `계산하기 (생수 950원)` (`IGCheckoutCounter.cpp:32`). A Korean player reads a price mismatch as a bug, not a clue.\n- **No year.** CH01's shipped notice says `7월 26일 (금)` with no year, so the 요일 is unfalsifiable. The moment a year is printed, both 요일 labels become verifiably wrong. The internal chain 26 금 → 25 목 → 24 수 → 23 화 is self-consistent.\n- **Plain hyphens and regular spaces only.** `AIGHorrorHUD::InitializeKoreanFont` builds a single-typeface composite from `malgun.ttf` with no fallback face, and `SupportsKoreanText()` is Korean-on/Korean-off, not glyph-coverage-aware. No box-drawing characters, no ideographic spaces, no Hanja anywhere in CH02 copy.\n\n---\n\n## 5. Complete file manifest\n\n### New files (4)\n| Path | Encoding |\n|---|---|\n| `Source/IndieGame/Sequence/IGSecondMorningDirector.h` | UTF-8 **with BOM** |\n| `Source/IndieGame/Sequence/IGSecondMorningDirector.cpp` | UTF-8 **with BOM** |\n| `Source/IndieGame/Narrative/IGObjectiveProvider.h` | UTF-8 |\n| `Source/IndieGame/Narrative/IGObjectiveProvider.cpp` | UTF-8 |\n\n### Modified files (16)\n| Path | Change |\n|---|---|\n| `Config/DefaultGameplayTags.ini` | 8 new tags incl. `Chapter.CH02` |\n| `Source/IndieGame/Player/IGStressComponent.h` | `DarknessRate` 0.055 → 0.012 |\n| `Source/IndieGame/Player/IGFlashlightComponent.h/.cpp` | `GetEffectiveBeamFraction()` |\n| `Source/IndieGame/Player/IGPlayerCharacter.cpp` | darkness early-out gated on beam fraction, not `bOn` |\n| `Source/IndieGame/Player/IGHorrorHUD.h/.cpp` | `ObjectiveDirector` via `IIGObjectiveProvider` |\n| `Source/IndieGame/Sequence/IGMorningRoutineDirector.h` | implements `IIGObjectiveProvider` |\n| `Source/IndieGame/Interaction/IGElevator.h/.cpp` | `ResetRide`, `SetIntermediateStop`, `OnFloorReached`, 3 states, 2 Tick cases |\n| `Source/IndieGame/Interaction/IGSwingDoor.h/.cpp` | `SetOpenState(bool, bool)` |\n| `Source/IndieGame/Interaction/IGFridge.h/.cpp` | `SetInspectedStateTag`, `SetInspectionThought`, `ResetInspection` |\n| `Source/IndieGame/Interaction/IGCheckoutCounter.h/.cpp` | `SetStoryTags`, `SetPurchaseThought` |\n| `Source/IndieGame/Sequence/IGDemoDirector.cpp` | guard: no loop reset while the capture tour is alive |\n| `Source/IndieGame/Core/IGPrologueWorldScene.h` | 9 new members, `BuildMirrorUnit` / `BuildChapterTwoOverlay` / `BuildChapterTwoFootprints` / `SpawnMorningActors` / `SpawnWallet` / `SpawnWaterBottles`, and the small public CH02 surface |\n| `Source/IndieGame/Core/IGPrologueWorldScene.cpp` | all of the above; **UTF-8 with BOM already, keep it** |\n| `Scripts/generate_surface_textures.py` | `M_WetStep` source texture |\n| `Scripts/create_textured_materials.py` | `M_WetStep` material |\n| `Docs/IMPLEMENTATION_STATUS.md` | CH02 status; record that CH02 autosaves are deliberately disabled pending the load coordinator |\n\n---\n\n## 6. Standing traps — re-read before each commit\n\n1. **UTF-8 with BOM** on every file carrying Korean literals. `Scripts/Validate-Project.ps1` enforces it; MSVC under a Korean locale otherwise decodes CP949 and truncates (`냉장고 닫기` → `닫`).\n2. **`MaterialNames[]`** (`IGPrologueWorldScene.cpp:404-437`) — `M_WetStep` must be appended or `TexMat` silently returns the fallback with no warning.\n3. **`CorridorFlickerHandle`** — 0.09 s repeating timer for the whole session writing unconditionally to `DegradedCorridorLight`, which *is* fixture 0. Suspend it before touching any corridor light.\n4. **Emissive discs** — every `CreateLight` in the corridor and the lobby has a separate `LightPanelMaterial` disc at local Z 234.5 whose component is currently discarded. Kill the light without the disc and you ship a glowing ring on a black ceiling.\n5. **Base-intensity snapshots** — take them exactly once, before any dimming.\n6. **Hidden ≠ non-colliding** — `CreateBlock` sets `BlockAll` and `SetVisibility(false)` leaves collision behind. Always flip both.\n7. **`ClearStates(false)`, never `true`** — a broadcasting clear re-enters every listener mid-swap. And destroy the CH01 director *before* the clear.\n8. **`HomeDoor->SetRequirements`** after `ClearStates` is a hard softlock if skipped, not a polish item.\n9. **No figure, ever** — no skeletal mesh, no `ACharacter::Mesh`, no animation code, and `Scripts/generate_meshes.py` is 16 grocery/furniture props. The sleeper is four blocks and a sphere; it never moves, is never directly lit, and is never approached.\n10. **Build with `-MaxParallelActions=3`.** The overlay adds ~120 hidden static components; measure `BeginPlay` before CH03 stacks another overlay on top.",
    "verdicts": [
      {
        "angle": "lore",
        "verdict": "usable"
      },
      {
        "angle": "systems",
        "verdict": "weak"
      },
      {
        "angle": "level",
        "verdict": "weak"
      },
      {
        "angle": "pacing",
        "verdict": "weak"
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Survey"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Critique"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Synthesise"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "survey:codebase",
      "phaseIndex": 1,
      "phaseTitle": "Survey",
      "agentId": "a32dab29e17fd2487",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784998687891,
      "queuedAt": 1784998684286,
      "attempt": 1,
      "lastToolName": "Grep",
      "lastToolSummary": "C:/2026_Project/06_unreal/Source",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999133355,
      "tokens": 190503,
      "toolCalls": 53,
      "durationMs": 445462,
      "resultPreview": "Absolute paths used below are all under `C:/2026_Project/06_unreal`.\n\n---\n\n# 1. World coordinate layout\n\n**Frame of reference.** One transient actor, `AIGPrologueWorldScene`, builds everything at `BeginPlay` (spawned unconditionally by `AIGPrologueGameMode::StartPlay`, `Source/IndieGame/Core/IGPrologueGameMode.cpp:27`, into `/Game/Maps/Prologue_Morning`, which is an otherwise empty map holding onl…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "design:lore",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a572830f99ede971b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999141011,
      "queuedAt": 1784999135104,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The dread is administrative and domestic, not supernatural.…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999960938,
      "tokens": 142683,
      "toolCalls": 25,
      "durationMs": 819927,
      "resultPreview": "{\"angle\":\"The dread is administrative and domestic, not supernatural. Nothing chases the player. The building is simply keeping records about a resident who has no name, no unit number and no next of kin — and the player keeps walking past those records to buy water. Every instrument is one a Korean player reads fluently without explanation: 층간소음 arriving from a floor that does not exist (you are …"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "design:systems",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a9ae5f01c534a1afa",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999139302,
      "queuedAt": 1784999135104,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The loop is literal and in-session: no map reload, no secon…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999687046,
      "tokens": 121118,
      "toolCalls": 31,
      "durationMs": 547744,
      "resultPreview": "{\"angle\":\"The loop is literal and in-session: no map reload, no second map, no second room. Chapter two reuses chapter one's exact geometry and closes one hole in the fiction — 403 *is* 404, reached through a door that has always been in the wall — so the horror is a fact the player can verify from both sides rather than a trick that breaks when inspected. Three beats only, each of which drives fe…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "design:level",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "aa647dd1650b1cf7b",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999140488,
      "queuedAt": 1784999135105,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The building repeats before the morning does. Every CH02 be…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999747508,
      "tokens": 156442,
      "toolCalls": 32,
      "durationMs": 607020,
      "resultPreview": "{\"angle\":\"The building repeats before the morning does. Every CH02 beat is the same object emitted twice — 403 is a copy of 404, the 2F landing is a copy of the 4F landing, the till holds a copy of your receipt, the puddle holds a copy of you — and every copy is wrong by exactly one step in time (one beat late, or seven minutes early). Nothing new is modelled; the horror is that the map has starte…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "design:pacing",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a954a6672292aeca1",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999141089,
      "queuedAt": 1784999135105,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "THE BUILDING IS ALREADY FINISHED WITH YOU. Chapter two is n…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999546534,
      "tokens": 79748,
      "toolCalls": 17,
      "durationMs": 405443,
      "resultPreview": "{\"angle\":\"THE BUILDING IS ALREADY FINISHED WITH YOU. Chapter two is not \\\"the same morning, but scary\\\" — it is the same morning arriving a step ahead of the player. Every wrong thing is something that has already happened: a light that went out while you weren't looking, a wallet already on a desk, a receipt already printed, footprints already walking home. Nothing chases the player because nothi…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "critique:pacing",
      "phaseIndex": 3,
      "phaseTitle": "Critique",
      "agentId": "addb3ec09713b6156",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999550014,
      "queuedAt": 1784999546536,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Pacing / stress-curve authorship: chapter two as a \"stairca…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1784999986905,
      "tokens": 161953,
      "toolCalls": 36,
      "durationMs": 436891,
      "resultPreview": "{\"angle\":\"Pacing / stress-curve authorship: chapter two as a \\\"staircase\\\" of lit lulls and dark floors, with every wrong thing already completed before the player arrives, driven by IGStressComponent's darkness/recovery/threat rates.\",\"fatalFlaws\":[\"THE CURVE IS WRONG BY A FACTOR OF 60 AND THE ANGLE IS THE CURVE. IGStressComponent.cpp:85 is `Stress += Darkness * DarknessRate * DeltaSeconds` with …"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "critique:systems",
      "phaseIndex": 3,
      "phaseTitle": "Critique",
      "agentId": "a9e03639c51877c75",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999691409,
      "queuedAt": 1784999687052,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "\"In-session loop, no second room: 403 IS 404, and fear come…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1785000304396,
      "tokens": 183222,
      "toolCalls": 36,
      "durationMs": 612987,
      "resultPreview": "{\"angle\":\"\\\"In-session loop, no second room: 403 IS 404, and fear comes from the stress model's physics rather than scripted numbers.\\\" All three pillars fail against the code. The loop reset as specified locks the player inside unit 404; making 403 literally be 404 destroys the reveal it exists to deliver; and the fear is ~90% hard-coded ApplyScare values by the proposal's own numbers. What survi…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "critique:level",
      "phaseIndex": 3,
      "phaseTitle": "Critique",
      "agentId": "a546bce50657339cc",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999754005,
      "queuedAt": 1784999749102,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "\"The building repeats before the morning does\" — every CH02…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1785000388011,
      "tokens": 160162,
      "toolCalls": 33,
      "durationMs": 634006,
      "resultPreview": "{\"angle\":\"\\\"The building repeats before the morning does\\\" — every CH02 beat is an existing CH01 component hidden and re-emitted at an offset, flipped on at a loop boundary. The idea is genuinely good and the survey work behind it is unusually accurate about the codebase. The execution is not: five of the seven beats are specified against class APIs that do not exist, and the two headline set-piec…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "critique:lore",
      "phaseIndex": 3,
      "phaseTitle": "Critique",
      "agentId": "a153dfed3912661a1",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1784999969268,
      "queuedAt": 1784999962506,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Lore/administrative-dread angle — \"the building is quietly …",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1785000590167,
      "tokens": 168098,
      "toolCalls": 39,
      "durationMs": 620898,
      "resultPreview": "{\"angle\":\"Lore/administrative-dread angle — \\\"the building is quietly completing the paperwork of a 고독사 around the player.\\\" The THESIS is the strongest thing here and should survive: culturally-native instruments (소금, 부적, 상 with the upright spoon, 대형폐기물 스티커, a 관리사무소 sheet whose worst line is a statement of procedural helplessness) are nearly all buildable from CreateBlock + one texture, need no f…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "synthesise:plan",
      "phaseIndex": 4,
      "phaseTitle": "Synthesise",
      "agentId": "a8022e8b5ba0bf98d",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785000598564,
      "queuedAt": 1785000592155,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd \"C:/2026_Project/06_unreal\" && grep -n \"GetStress\\|GetFl…",
      "promptPreview": "IMPORTANT CONSTRAINTS:\n- You are READ-ONLY. Do NOT edit, write, or create any file. Do NOT run builds\n  (Build.bat), do NOT run UnrealEditor, do NOT run any PowerShell script.\n  Another process is building this project concurrently and you would corrupt it.\n- You may use Read, Grep, Glob only.\n- Repository root: C:/2026_Project/06_unreal\n\n\nPROJECT: A first-person Korean-setting horror game in Unre…",
      "lastProgressAt": 1785001466514,
      "tokens": 253148,
      "toolCalls": 36,
      "durationMs": 867949,
      "resultPreview": "# Chapter Two — 두 번째 아침\n## Implementation Plan (synthesised from all four proposals + reviews)\n\nAll paths are absolute under `C:/2026_Project/06_unreal`. Every code claim below was checked against the current source; line numbers cited are from the files as they exist today.\n\n---\n\n## 0. The one-sentence design\n\n> The morning restarts in-session at the villa's front door. The building has kept ever…"
    }
  ],
  "totalTokens": 1617077,
  "totalToolCalls": 338
}